apiVersion: v1
kind: ConfigMap
metadata:
  name: flask-app-config
  namespace: flask-app
  labels:
    app.kubernetes.io/name: flask-app
    app.kubernetes.io/component: config
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  APP_VERSION: "1.0.0"
